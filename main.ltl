// ltl-base64/main.ltl
//
// RFC 4648 base64 / base32 / base16 CLI + library.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-base64 v0.1.0")
    io::println("RFC 4648 base64 / base32 / base16 CLI + library.")
    0
}
